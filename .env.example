# Example environment variables for Vite
# Copy this to .env and fill in your values

# ============================================================
# Laravel API Configuration
# ============================================================

# Base URL for your Laravel API (no trailing slash)
VITE_API_BASE=https://your-backend-url.com/api

# ============================================================
# Application Settings
# ============================================================

VITE_APP_NAME=SmartPath Dashboard

# Environment mode: development, staging, production
VITE_APP_ENV=development

# Enable logging (true/false) - set to false in production
VITE_ENABLE_LOGGING=true

# ============================================================
# Network Settings
# ============================================================

# Retry configuration for network errors (ERR_CONNECTION_RESET)
# Max retries before giving up (default: 3)
VITE_API_MAX_RETRIES=3

# Initial retry delay in milliseconds (uses exponential backoff)
# Example: 1000ms, 2000ms, 4000ms for 3 retries
VITE_API_RETRY_DELAY=1000

# ============================================================
# Notes
# ============================================================
# - Access tokens stored in localStorage
# - Refresh tokens stored in HTTP-only cookies (handled by backend)
# - API wrapper (src/lib/api.js) handles auto-refresh on 401
# - Never commit .env file with real secrets to git
# - VITE_ENABLE_LOGGING=false will disable all console logs except errors

