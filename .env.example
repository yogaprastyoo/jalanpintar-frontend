# Example environment variables for Vite (do NOT commit a real .env with secrets)
# Copy this to .env.local and fill in real values on your machine.

# ============================================================
# Laravel API Configuration (Laravel 12 + Sanctum)
# ============================================================

# Base URL for your Laravel API (no trailing slash)
VITE_API_BASE=https://api.example.com

# Auth endpoints for Laravel Sanctum token-based auth
# NOTE: Update these to match your actual Laravel routes
VITE_API_LOGIN=login
VITE_API_REGISTER=register
VITE_API_REFRESH=refresh
VITE_API_LOGOUT=logout

# Accept header for JSON API responses
VITE_API_ACCEPT=application/json

# ============================================================
# Optional: Payment Integration
# ============================================================

# Stripe publishable key (if you integrate payments client-side)
VITE_STRIPE_PK=pk_test_xxx

# ============================================================
# Feature Flags / App Settings
# ============================================================

VITE_ENABLE_UPSELL=true
VITE_APP_NAME=SmartPath

# Analytics / monitoring (optional)
VITE_GA_ID=G-XXXXXXX

# ============================================================
# Notes for Laravel Sanctum Auth
# ============================================================
# - Access tokens are stored in localStorage (or httpOnly cookie if Laravel SPA mode)
# - Refresh tokens stored in localStorage and used to renew access tokens
# - The frontend API wrapper (src/lib/api.js) handles:
#   1. Attaching Bearer token to requests
#   2. Auto-refresh when 401 Unauthorized
#   3. Logout and token cleanup
# - Never commit a .env file with real secrets. Use .env.example as a template.
